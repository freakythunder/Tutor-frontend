import React from 'react';
import styles from '../Styles/IDE.module.css';

const IDE: React.FC = () => {
    return (
        <div className={styles.ide}>
            <p className={styles.text}>\\ Write a for loop for iterating through the following array.</p>
        </div>
    );
}

export default IDE;
